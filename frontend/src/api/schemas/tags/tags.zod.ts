/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Jx Finance API
 * Personal and household finance ledger. Every route lives under /api and answers JSON. Money is carried as a decimal string with at most two decimal places so nothing is lost to floating point; dates are YYYY-MM-DD in the instance time zone. Collections that can grow are paged with page and pageSize and answer with items, page, pageSize, and total. Authentication is a session cookie from POST /api/auth/login, so browser clients must send credentials. Failures answer application/problem+json with a machine-readable code per error; see the ProblemDetails schema.
 * OpenAPI spec version: v1
 */
import * as zod from "zod";

/**
 * Adds a tag that transactions can be marked with. A tag says what a payment was for across categories — a holiday, a renovation, something to be reimbursed — so it has no flow type and the same tag can sit on income and on an expense. Names are unique per owner, ignoring case.
 * @summary Create a tag
 */
export const createTagBodyNameMin = 0;
export const createTagBodyNameMax = 50;

export const CreateTagBody = zod.object({
  name: zod
    .string()
    .min(createTagBodyNameMin)
    .max(createTagBodyNameMax)
    .describe("Display name, unique among your own tags whatever the casing."),
  scope: zod.enum(["personal", "shared"]),
  householdId: zod
    .uuid()
    .nullable()
    .describe("Required when Scope is Shared; must be a household you belong to."),
});

export const CreateTagResponse = zod.object({
  id: zod.uuid(),
  name: zod.string(),
  scope: zod.enum(["personal", "shared"]),
  householdId: zod.uuid().nullable(),
});

/**
 * Returns your personal tags plus the shared tags of the households you belong to, by name. A tag has no flow type, so the same list serves income and expense. While a household is active, the tags of the other households are left out, exactly as categories are.
 * @summary List tags
 */
export const TagsResponseItem = zod.object({
  id: zod.uuid(),
  name: zod.string(),
  scope: zod.enum(["personal", "shared"]),
  householdId: zod.uuid().nullable(),
});
export const TagsResponse = zod.array(TagsResponseItem);

/**
 * Removes the tag and takes it off every transaction that carried it, the way deleting a category leaves its transactions uncategorised. The transactions themselves, their amounts, categories and split lines are untouched. Only the owner can delete a shared tag.
 * @summary Delete a tag
 */
export const DeleteTagResponse = zod.void();

/**
 * Renames a tag or moves it between personal and shared. A household member can rename a shared tag; only its owner can change the sharing or delete it. The transactions already marked with the tag keep it.
 * @summary Update a tag
 */
export const updateTagBodyNameMin = 0;
export const updateTagBodyNameMax = 50;

export const UpdateTagBody = zod.object({
  name: zod.string().min(updateTagBodyNameMin).max(updateTagBodyNameMax),
  scope: zod.enum(["personal", "shared"]),
  householdId: zod.uuid().nullable(),
});

export const UpdateTagResponse = zod.object({
  id: zod.uuid(),
  name: zod.string(),
  scope: zod.enum(["personal", "shared"]),
  householdId: zod.uuid().nullable(),
});
