/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Jx Finance API
 * Personal and household finance ledger. Every route lives under /api and answers JSON. Money is carried as a decimal string with at most two decimal places so nothing is lost to floating point; dates are YYYY-MM-DD in the instance time zone. Collections that can grow are paged with page and pageSize and answer with items, page, pageSize, and total. Authentication is a session cookie from POST /api/auth/login, so browser clients must send credentials. Failures answer application/problem+json with a machine-readable code per error; see the ProblemDetails schema.
 * OpenAPI spec version: v1
 */
import * as zod from "zod";

/**
 * Provisions a user account with an initial password. There is no self-service sign-up: accounts exist only because an administrator created them. Administrators only.
 * @summary Create a user
 */
export const createUserBodyEmailMin = 0;
export const createUserBodyEmailMax = 256;

export const createUserBodyEmailRegExp = new RegExp("^[^@]+@[^@]+$");
export const createUserBodyDisplayNameMin = 0;
export const createUserBodyDisplayNameMax = 100;

export const createUserBodyPasswordMin = 8;
export const createUserBodyPasswordMax = 100;

export const CreateUserBody = zod.object({
  email: zod
    .email()
    .min(createUserBodyEmailMin)
    .max(createUserBodyEmailMax)
    .regex(createUserBodyEmailRegExp),
  displayName: zod.string().min(createUserBodyDisplayNameMin).max(createUserBodyDisplayNameMax),
  role: zod.string().describe("Admin or Member."),
  password: zod
    .string()
    .min(createUserBodyPasswordMin)
    .max(createUserBodyPasswordMax)
    .describe("Initial password; the user can change it from their profile."),
});

export const CreateUserResponse = zod.object({
  id: zod.uuid(),
  email: zod.string(),
  displayName: zod.string(),
  role: zod.string(),
  twoFactorEnabled: zod.boolean(),
  isActive: zod.boolean(),
});

/**
 * Returns user accounts with their role and whether they are still active. Filters are optional and combine with AND. Administrators only.
 * @summary List users
 */
export const UsersResponseItem = zod.object({
  id: zod.uuid(),
  email: zod.string(),
  displayName: zod.string(),
  role: zod.string(),
  twoFactorEnabled: zod.boolean(),
  isActive: zod.boolean(),
});
export const UsersResponse = zod.array(UsersResponseItem);

/**
 * Changes your display name and, optionally, your password. A password change needs the current password as well, and refreshes the session cookie so the browser stays signed in. This is the one user endpoint that does not require the Admin role.
 * @summary Update your own profile
 */
export const updateMyProfileBodyDisplayNameMin = 0;
export const updateMyProfileBodyDisplayNameMax = 100;

export const updateMyProfileBodyNewPasswordMin = 8;
export const updateMyProfileBodyNewPasswordMax = 100;

export const UpdateMyProfileBody = zod.object({
  displayName: zod
    .string()
    .min(updateMyProfileBodyDisplayNameMin)
    .max(updateMyProfileBodyDisplayNameMax),
  currentPassword: zod.string().nullable().describe("Required only when newPassword is supplied."),
  newPassword: zod
    .string()
    .min(updateMyProfileBodyNewPasswordMin)
    .max(updateMyProfileBodyNewPasswordMax)
    .nullable()
    .describe("Omit to leave the password alone."),
});

export const UpdateMyProfileResponse = zod.object({
  id: zod.uuid(),
  email: zod.string(),
  displayName: zod.string(),
  role: zod.string(),
  twoFactorEnabled: zod.boolean(),
  isActive: zod.boolean(),
});

/**
 * Locks the account out instead of deleting it, so the transactions and households it touched stay intact. Existing sessions are rejected on their next request. You cannot deactivate yourself, and the last active administrator cannot be deactivated, so an instance is never left without one. Administrators only.
 * @summary Deactivate a user
 */
export const DeactivateUserResponse = zod.void();

/**
 * Lifts the deactivation set by the deactivate operation and clears the failed sign-in counter, so the user can sign in again with the password they had. Sessions from before the deactivation stay revoked. Calling it for a user who is already active changes nothing and still answers 204, so it is safe to repeat. Administrators only.
 * @summary Reactivate a deactivated user
 */
export const ReactivateUserResponse = zod.void();

/**
 * Administrators only. For a user who forgot their password: the administrator chooses a temporary password and hands it over outside the application, and the user changes it on their profile after signing in. The administrator confirms the action with their own current password; a wrong one answers password.incorrect, counts toward the administrator's sign-in lockout, and a locked-out administrator answers credentials.lockedOut. The new password must satisfy the same rules as any other password, otherwise password.tooWeak. Every session of the target user is revoked and their failed sign-in counter and temporary lockout are cleared; a deactivated user stays deactivated. With resetTwoFactor the authenticator is switched off and the recovery codes are invalidated as well, for a user who also lost their device. The target may be a member or another administrator, never the caller: change your own password on the profile. Rate limited to 10 calls per five minutes per client.
 * @summary Set a new password for another user
 */
export const resetUserPasswordBodyNewPasswordMin = 8;
export const resetUserPasswordBodyNewPasswordMax = 100;

export const resetUserPasswordBodyCurrentPasswordMin = 0;
export const resetUserPasswordBodyCurrentPasswordMax = 100;

export const resetUserPasswordBodyResetTwoFactorDefault = false;

export const ResetUserPasswordBody = zod.object({
  newPassword: zod
    .string()
    .min(resetUserPasswordBodyNewPasswordMin)
    .max(resetUserPasswordBodyNewPasswordMax)
    .describe("The temporary password for the user, 8 to 100 characters."),
  currentPassword: zod
    .string()
    .min(resetUserPasswordBodyCurrentPasswordMin)
    .max(resetUserPasswordBodyCurrentPasswordMax)
    .describe("The current password of the signed-in administrator."),
  resetTwoFactor: zod
    .boolean()
    .default(resetUserPasswordBodyResetTwoFactorDefault)
    .describe(
      "Also switch off two-factor authentication and invalidate the recovery codes. Default false.",
    ),
});

export const ResetUserPasswordResponse = zod.object({
  id: zod.uuid(),
  email: zod.string(),
  displayName: zod.string(),
  role: zod.string(),
  twoFactorEnabled: zod.boolean(),
  isActive: zod.boolean(),
});

/**
 * Promotes a user to administrator or demotes them to member. You cannot demote yourself, and the last active administrator cannot be demoted, so an instance is never left without one. Role changes and deactivations are applied one at a time, so two administrators demoting each other at the same moment cannot both succeed. Administrators only.
 * @summary Change a user role
 */
export const UpdateUserRoleBody = zod.object({
  role: zod.string().describe("Admin or Member."),
});

export const UpdateUserRoleResponse = zod.object({
  id: zod.uuid(),
  email: zod.string(),
  displayName: zod.string(),
  role: zod.string(),
  twoFactorEnabled: zod.boolean(),
  isActive: zod.boolean(),
});
