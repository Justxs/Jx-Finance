# Budgets

Back to the [feature walkthrough](<../14. Feature walkthrough with diagrams.md>).

Backend `Budgets`, page `/budgets`. Monthly limit per expense category in the reporting currency, personal only.

```mermaid
flowchart LR
    Unsplit["Unsplit expense<br/>own category, ReportingAmount"] --> Attr["ICategoryAttributionService<br/>single projection"]
    Split["Split expense<br/>each line's category and share"] --> Attr
    Attr --> Spent["Spent per category, current calendar month<br/>installation time zone"]
    Limit["Budget.Limit"] --> Usage["BudgetResponse: limit, spent<br/>Meter and BudgetUsageChart"]
    Spent --> Usage
    Attr --> Dash["Dashboard category breakdown"]
    Attr --> Rep["Reports category breakdown"]
```
