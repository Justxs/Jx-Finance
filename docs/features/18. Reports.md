# Reports

Back to the [feature walkthrough](<../14. Feature walkthrough with diagrams.md>).

Backend `Reports`, page `/reports`. One endpoint, `GET /api/reports/summary`, for any date range; the year presets use the same report.

```mermaid
flowchart LR
    Range["DateRangePicker and presets<br/>dateFrom, dateTo in the URL"] --> Api["GET /api/reports/summary"]
    Api --> Totals["Income, expense, net<br/>sum of ReportingAmount"]
    Api --> Break["Category breakdown through ICategoryAttributionService"]
    Api --> Trend["Trend over the range"]
    Break --> Drill["Category row links to /transactions<br/>with category and date range"]
```
