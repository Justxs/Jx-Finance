# Core data model

Back to the [feature walkthrough](<../14. Feature walkthrough with diagrams.md>).

```mermaid
erDiagram
    AppUser ||--o{ UserSession : "signed-in browsers"
    AppUser ||--o{ Account : owns
    AppUser ||--o{ Category : owns
    AppUser ||--o{ Budget : owns
    AppUser ||--o{ Goal : owns
    AppUser ||--o{ Asset : owns
    AppUser ||--o{ Debt : owns
    AppUser ||--o{ RecurringBill : owns
    AppUser ||--o{ Notification : receives
    AppUser ||--o{ NetWorthSnapshot : "one per local day"
    AppUser ||--o{ HouseholdMembership : "Owner or Member"
    Household ||--o{ HouseholdMembership : has
    Household ||--o{ Account : "shared scope"
    Household ||--o{ Category : "shared scope"
    Account ||--o{ Transaction : holds
    Transaction ||--o{ TransactionLine : "optional split"
    Category ||--o{ Transaction : categorizes
    Category ||--o{ TransactionLine : categorizes
    Category ||--o{ Budget : limits
    Account ||--o{ Transfer : "from or to"
    Transfer ||--o{ TransferImport : "bank entry receipt, one per account"
    Account ||--o{ CurrencyConversion : "in-account exchange"
    CurrencyConversion |o--o| Transaction : "fee expense"
    Account ||--o{ InvestmentTransaction : holds
    Security ||--o{ InvestmentTransaction : traded
    Account ||--o| BrokerConnection : "Flex Query sync"
    RecurringBill }o--o| Account : "default account"
    RecurringBill }o--o| Category : category
    ExchangeRate {
        date Date
        string Currency
        decimal UnitsPerEuro
    }
    InstanceSettings {
        int Id "always 1"
        string ReportingCurrency
        bool FeatureFlags "one column each"
    }
```

Money is `decimal` and `numeric(18,2)`, travels as a dot-decimal string, and rounds half away from zero. Balances, holdings, cost basis and gains are never stored; they are computed from the rows. See [Data model](<../5. Data model.md>).
