# Backlog and ideas

What is not done yet. Written 2026-09-20 from [Features and scope](<2. Features.md>), [Current decisions](<9. Open questions & decisions.md>), the [release checklist](<11. Milestones & tasks.md>) and the [feature walkthrough](<14. Feature walkthrough with diagrams.md>). Nothing here is a promise; each item needs its own scoped change. Size is a rough guess: S is days, M is a week or two, L is more.

## 1. Open release work

These come from the release checklist and block calling the current release verified.

| Item | Note |
| --- | --- |
| Production overlay on the real host | Certificate trust on client devices, real `SITE_ADDRESS` and `BIND_ADDRESS` |
| Rerun `just verify-production` | The API container did not start on the 2026-09-19 working tree because of pending model changes |
| First run of the new CI jobs on the Gitea runner | Audit, `production-overlay`, client drift |
| `--recover-admin` invoked inside the container | Only run on the host so far |
| Fresh verification pass | Evidence in Verification predates multi-currency, investments, settings and backups |
| Sustained daily-use trial | Cannot be inferred from automated checks |

## 2. Gaps inside features that exist

| Feature | Gap | Size |
| --- | --- | --- |
| Investments | Corporate actions other than splits are only counted (mergers, spin-offs, symbol changes) | M |
| Investments | Bonds by hand only; options, futures and short positions out of scope | L |
| Investments | Only Interactive Brokers; no other broker and no generic trade CSV | M |
| Import | Swedbank format only | M per bank, or one generic column mapper |
| Import | No credit-card statements | M |
| Households | Budgets, goals, assets, debts and bills cannot be shared | L |
| Users | No forced password change after an administrator reset (rejected on 2026-09-19 unless done completely) | M |
| Settings | Installation-wide only; no per-user reporting currency, language default or page size | L for currency, S for the rest |
| Exchange rates | ECB only; no manual rate, no currencies outside the ECB list | S |
| Backups | On demand only; a scheduler and offsite copies were removed on 2026-09-05 and stay a deliberate decision | M, needs the decision reopened |

## 3. Deferred features named in the docs

| Feature | What it needs | Size |
| --- | --- | --- |
| Live investment prices | A market data source and a daily job that fills the price history, which exists since 2026-09-20; today a price arrives only from the broker import or by hand | M |
| PWA and offline | Manifest, service worker, offline queue for new transactions | L |
| Bank APIs | PSD2 aggregator; licensing and consent renewal make this costly for a household tool | L |

## 4. New ideas

Ordered by how much they would help daily use for the effort.

| Idea | Why | Size |
| --- | --- | --- |
| Cash-flow forecast | Project the next 30 to 90 days of each account from the active recurring entries of all three shapes; shows a coming shortfall before it happens | M |
| Receipts and attachments | A file per transaction on a volume, included in backups | M |
| Audit log | Who changed or deleted what in a shared household account; soft deletion already keeps the rows, and `DeletionEntries` now records every delete a person made, which is the first half of it | M |
| Debt amortization | Debts already store an interest rate; add a payment schedule, payoff date and the interest and principal split of each payment | M |
| Dashboard customisation | Choose and order the cards per user | M |
| Generic CSV import | The user maps columns once per bank and the mapping is stored; covers SEB, Luminor, Revolut and Wise without code per bank | M |
| Passkeys | WebAuthn as a second factor or instead of the password | M |
| Personal API tokens | Read-only tokens for scripts and spreadsheets | M |
| Data export per user | A member takes their own records out as JSON or CSV without an administrator backup | M |

## Suggested order

1. Section 1, because the release is not verified without it.
2. A generic CSV import, now that categorization rules are in; between them they remove the most manual work.
3. Cash-flow forecast, now that recurring income and transfers exist to project.
4. Anything that depends on email, now that email is delivered: a weekly or monthly summary, a budget alert by mail, an invitation flow if public registration is ever wanted.

## Done

| Date | Item | Where it is described |
| --- | --- | --- |
| 2026-09-21 | Categories, tags, categorization rules and households in the trash: each of those deletes now records the rows it rewrote — the transactions, split lines and recurring entries a category was cleared from and the budgets it retired, the transactions a tag was on, the tags of a rule, the accounts, categories and tags that were shared into a household — as `DeletionChanges` beside its trash entry, in the same database transaction, and raises the undo toast. The trash row says what will come back, for example `Groceries, 42 transactions, 1 budget`. A restore applies each recorded row only where it is still sound: a transaction given another category since keeps it, a budget whose slot is taken stays deleted, a tag goes back only onto transactions still stored, a rule returns to its old position clamped to the end, and a household re-shares only rows still personal and owned by a member. It is refused with the new `restore.nameTaken` when a tag's name was taken again, `collection.invalidSize` at the rule limit and `access.forbidden` when the restorer is no longer an owner of the household. The active-household header now ignores a deleted household | [Trash and undo](<features/29. Trash and undo.md>) |
| 2026-09-21 | Investment entries in the trash: deleting a buy, sell, split, dividend, withholding tax, interest or fee writes a trash entry, raises the undo toast on the investments activity list and lists it as, for example, `Sell 3 MSFT, 2026-07-15`. A restore replays the holding first in first out with the entry back in its place by date, through the same oversold check the create, edit and delete paths use, now shared as `IHoldingLedger`, and is refused with `holding.oversold` when the restored sale itself would sell more than was held, with `holding.dependentSales` when a later sale now needs its shares, with `restore.referenceMissing` when the account is archived and with the new `restore.securityChanged` when the security of a restored trade has another currency | [Investments](<features/20. Investments.md>) |
| 2026-09-21 | Restoring an archived account: the accounts page lists the archived accounts the caller can see in a collapsed section under the table, and the owner restores one with a button on its row. Archiving only ever flipped the soft-delete flag, so a restore brings back everything posted to the account — transactions, transfers, conversions, investment entries, recurring entries and the progress of goals funded from it — with nothing to reconcile, and the list and the restore follow ownership, household sharing and the active household the way the account list does. Only the owner restores, a member sees the row without the button, and a repeat is a no-op. Removing a member or deleting a household now makes that owner's archived accounts personal too, as it always did for active ones, and an older row still shared into a household its owner left comes back personal | [Accounts](<features/05. Accounts.md>) |
| 2026-09-21 | Exports follow the active household: the three file downloads carry the scope in an `activeHousehold` query parameter as well as in the header, so a CSV taken from a plain link holds the rows the screen holds and agrees with the PDF of the same screen. The same middleware reads both, against the same memberships, and a request naming two different households is refused | [Exports](<features/19. Exports.md>), [Households and sharing](<features/16. Households and sharing.md>) |
| 2026-09-21 | Command palette: one dialog over the whole application on `Ctrl+K`, `⌘K` on macOS, written once as `Mod+K` and the only shortcut in the scheme that carries a modifier, because every bare key here is deliberately ignored while a field has focus and a palette has to be reachable from the middle of a form. It offers three kinds of entry: a page or one of the sections that are search parameters rather than routes — the trash, the signed-in browsers, the import, the appearance, the investments tax summary, and the feature, email and backup sections of settings; a record, meaning any account, category or tag the caller can see, which opens the ledger filtered to it; and an action that already exists — a new transaction, transfer or account through the URL so the back button undoes it, a backup for an administrator, signing out, the theme, the language and the active household. Every row names the feature switch or the administrator role it needs, checked against the same settings and profile the sidebar and the route guards use, so nothing offered can answer a 404 or a redirect. Matching folds case and accents away and falls back from an exact name through a prefix, a word start and a substring to a subsequence, with keyword matches always behind label matches and the last eight choices, kept in the existing `jx-preferences` row, breaking ties. Nothing is loaded until it first opens, when it asks for the accounts, the categories, the tags and, only while that switch is on, the households — all four lists some page already loads, held for five minutes — and no endpoint was added. The dialog is a labelled combobox over a listbox with active-descendant handling, arrow keys, `Home`, `End`, `Enter` and `Escape`, an empty state that removes the listbox rather than leaving an empty one, and a spoken count | [Interface](<features/24. Interface.md>) |
| 2026-09-21 | Report comparisons: an optional second period beside the chosen one, asked for with `comparison=previousPeriod` (the same number of days immediately before the range) or `comparison=previousYear` (the same range a year earlier, where a range ending on the last day of a month again ends on a month end, so February meets the whole of February and a leap day neither invents nor loses a day). Every figure the report already answered gains its counterpart: the totals, each category, each synthetic investment group, each tag and each trend bucket, with the breakdowns merged on their key so something only one period touched is one entry with zero on the empty side, ordered by the larger of the two amounts so a category that fell to nothing keeps its place. The two periods are read with the same number of database round trips as one — every range query carries both windows in one predicate and the rows are split by date in memory — and the report is in fact one query cheaper than before, because the totals now come from the grouped query the trend already ran. The difference and its percentage are computed on the client in one tested function, and a zero base is stated as "up from nothing" rather than an infinity, while a fall to zero stays an ordinary −100%. Better and worse are carried by an arrow, an explicit sign and a spoken verdict, never by colour alone. Nothing changes without the parameter: the response, the query count and the CSV and PDF transaction exports are the ones they were | [Reports](<features/18. Reports.md>) |
| 2026-09-21 | Yearly tax summary for investments: a second view of the investments page showing one calendar year of what was recorded on the accounts the reader chooses, defaulting to every visible one. Every disposal is listed with its date, security, quantity, proceeds, first-in-first-out cost basis, gain or loss and the acquisition date, quantity and cost of each lot it consumed, beside every dividend, interest, withholding tax and standalone fee of the year; each figure appears in the reporting currency at the rate frozen on the entry's date and, when they differ, in the currency it was recorded in. `Position` now records the acquisition date of each lot and the lots a sale consumed, so the page reads the replay the portfolio already runs rather than repeating the rule. A CSV of the same rows is written the way the transaction export is, with a `Section` column and one row per consumed lot, and the printable view is the page under a print stylesheet rather than a second MigraDoc document. A year with nothing recorded answers an empty summary. It is stated plainly on the page and in the documentation, in both languages, that this is a summary of recorded data and not tax advice: no tax, rate or allowance is applied and no tax form is named | [Investments](<features/20. Investments.md>), [Exports](<features/19. Exports.md>) |
| 2026-09-21 | Subscription detection: the recurring entries page offers schedules it found by itself. A read-only analysis groups the caller's visible expenses of the last two years by account and by a description stripped of bank noise — trailing reference numbers, dates inside the text, varying case and repeated whitespace — in one tested pure function, and keeps a group only when it has three occurrences, consecutive gaps that all fit one cadence within its tolerance and amounts within 15% of their median, which it reports as the typical amount beside the cadence, the account, the category when every occurrence agrees, the occurrence dates and the next expected date. Groups an active recurring entry already covers, and groups the user dismissed, are left out; a dismissal is keyed to the group rather than to the transactions, so next month's payment does not bring it back. The query is bounded by the window, a row cap and a five-column projection, and the grouping is one pass over it. Creating an entry from a candidate opens the ordinary create form prefilled and writes through the existing create path | [Recurring entries](<features/13. Recurring bills.md>) |
| 2026-09-21 | Undo for delete: deleting a transaction, transfer, currency conversion, budget, goal, asset, debt or recurring entry writes a trash entry beside the soft delete, raises a toast with Undo from the one shared delete hook every screen already uses, and stays restorable for 30 days from a Trash section on the profile. A restored transaction brings its split lines and its tags with it and counts again in balances, budgets and reports; a restored conversion brings back its fee transaction. Restoring is keyed by the record rather than the trash row, so the toast and the screen call one operation, and is safe to repeat. It is refused with a published code when the account was archived, the category is gone, a conversion's fee was deleted on its own, a split's lines are no longer stored or another budget took the category and period. Categories, tags, categorization rules and households stay final, because their delete rewrites rows nothing recorded, and archiving an account stays its own operation. The 30-day window is a read filter and nothing purges | [Trash and undo](<features/29. Trash and undo.md>) |
| 2026-09-20 | Saved filters: the filter half of the ledger search params — text, account, category, tags, type and the date range — put under a name and reopened later, with renaming and deleting from the same menu in the page header. The page number, the sort and the row selection are deliberately left out, applying goes through the router like every other filter control so the back button still undoes it, and a saved filter that names a category, account or tag that no longer exists is applied exactly as stored while its entry says so. Kept per browser in a `jx-saved-filters` local-storage collection, so there is no table, no endpoint and nothing to invalidate | [Transactions](<features/07. Transactions.md>), [Interface](<features/24. Interface.md>) |
| 2026-09-20 | Transaction templates and duplicate: one action on a row opens the ordinary create dialog filled from it with today's date, its tags and its split lines, and writes nothing until the form is submitted; the same dialog can save its shape — account, category, tags, description, amount, flow type and split lines — under a name, and a named template starts a new entry the same way. Both go through the existing create form and its validation rather than a second write path, and templates are kept per browser in a `jx-transaction-templates` local-storage collection because a template is a draft of a form rather than a ledger record | [Transactions](<features/07. Transactions.md>), [Interface](<features/24. Interface.md>) |
| 2026-09-20 | Categorization rules: personal, ordered rules that read a transaction description — contains, starts with or equals, ignoring case — optionally narrowed to one account and an amount range, and set a category, a set of tags or both. The first matching rule decides a row. They fill a suggestion into the Swedbank import preview, which the user can still change and which never overwrites a choice already made, and they run on demand over the existing ledger with a per-rule preview and an explicit apply, touching only rows with no category unless a separate labelled option asks for a recategorization. Matching over the ledger happens in PostgreSQL, ordering is a dense integer position with up and down controls, and the whole feature sits behind its own switch | [Categorization rules](<features/28. Categorization rules.md>) |
| 2026-09-20 | Tags on transactions: a second classifier beside the category, owned and shared exactly like one with a name unique per owner ignoring case, many-to-many with the transaction and never with a split line. Tags are set on create and edit and on a selection of rows at once, the ledger filter keeps a row only when it carries every chosen tag and is served by an index rather than a query per row, the report breaks expenses down by tag with untagged spending as its own group, both exports carry a tag column, a screen manages them the way the categories screen does, and deleting a tag takes it off its transactions without touching them | [Tags](<features/27. Tags.md>) |
| 2026-09-20 | Email delivery: one SMTP server per installation, set by an administrator with a data-protected password no response carries and a test message that reports the server's own words; a single-use, one-hour password reset link from the sign-in screen whose answer is the same for an address that exists and one that does not; address confirmation with a resend, blocking nothing except unsolicited mail to that address; an optional per-user reminder email written beside the in-app notification in the same transaction; everything but the test message queued in an outbox and drained by a background job with timeouts, isolated failures and bounded retries, so no request and no other job waits for a mail server | [Email](<features/26. Email.md>) |
| 2026-09-20 | Households: a global active-household switcher in the sidebar and the phone header. The scope is "Everything" by default or one membership, it is kept per browser in `jx-preferences` and sent as the `X-Active-Household` header, the server validates it against the caller's memberships and ignores anything it cannot confirm, and the narrowing happens in the shareable query filter so transactions, transfers, conversions and investment entries follow their account. Personal records stay visible in every scope, a new account or category defaults to the active household, and nothing outside a request is scoped | [Households and sharing](<features/16. Households and sharing.md>) |
| 2026-09-20 | Recurring entries: a schedule now records an expense, an income or a transfer between two of your own accounts, each fixed or variable; the transfer confirmation reuses the ordinary transfer create path, so a cross-currency pair asks for the received amount exactly as a manual transfer does; editing changes the shape under the same rules and refuses one that would leave a required field empty; the reminder job, the dedupe rule and the notification treat all three alike, with the sentence picked from the shape in both locales | [Recurring entries](<features/13. Recurring bills.md>), [Notifications](<features/14. Notifications.md>) |
| 2026-09-20 | Notifications: a shared in-app channel instead of a bill-only one. The kind is an enum and the values the sentence needs are a typed payload column, rows written before it are read back unchanged, listing and clearing work whatever is switched on, a row whose producing feature was switched off stays listed without its link, and the bell localizes every kind in English and Lithuanian | [Notifications](<features/14. Notifications.md>) |
| 2026-09-20 | Budget alerts: an hourly job raises one in-app notification when a budget's spending reaches 80% of its effective limit and another at 100%, once per budget, window and threshold, from the same `BudgetUsageCalculator` the budgets page uses, and the alert opens the budgets page | [Budgets](<features/11. Budgets.md>), [Notifications](<features/14. Notifications.md>), [Background jobs](<features/23. Background jobs.md>) |
| 2026-09-20 | Budgets: weekly, monthly, quarterly and yearly windows in the installation time zone and from the configured first day of the week, one budget per category and period, and an optional rollover that carries the previous window's remainder (or overspend) into the current limit, walked back at most twelve windows or to the budget's creation | [Budgets](<features/11. Budgets.md>) |
| 2026-09-20 | Investments: a price per security and date, written by hand for today or a past date or by the broker import and deletable, plus a chart of the portfolio's value against its cost basis, computed per request | [Investments](<features/20. Investments.md>) |
| 2026-09-20 | Investments: dividends and interest count as income, withholding tax and standalone fees as expense, in reports and on the dashboard; shown as synthetic groups without a category link; budgets unchanged | [Reports](<features/18. Reports.md>), [Investments](<features/20. Investments.md>) |
| 2026-09-20 | Sessions: list of signed-in browsers on the profile with "Sign out" per browser and "Sign out everywhere else"; a revoked browser is refused on its next request | [Sign-in, sessions and lockout](<features/02. Sign-in, sessions and lockout.md>) |
| 2026-09-20 | Goals: a goal is funded either by hand as before or from one visible account, taking a whole percentage of its reporting balance at read time, clamped at zero and never stored; editing switches between the two and keeps the typed amount; an archived or no longer visible funding account leaves the goal listed with its progress unavailable | [Goals](<features/12. Goals.md>) |
